# Vyos Inventory
#

firewalls:
  hosts:
    <your firewall IP/FQDN>:
      ansible_host: <your ansible host IP/FQDN>
  vars:
    ansible_network_os: vyos.vyos.vyos
    ansible_connection: network_cli
    ansible_ssh_private_key_file: /path/to/your/ansible/user/privkey
    ansible_user: ansible
